{
  "note": "2026-07-21 update: environment now ships a CoreX build of OpenCV 4.6.0 under /usr/local/corex. The from-source OpenCV build path below (scripts/build_opencv_corex.sh + patches/corex/*) is SUPERSEDED and no longer required. Migration now = version-match gocv to v0.31.0 and link Go/CGO against the pre-installed CoreX OpenCV. No repo source changes are needed to build/test the bindings.",
  "active_approach": {
    "gocv_version": "v0.31.0 (worktree .tools/gocv-0.31, matches OpenCV 4.6.0)",
    "env": {
      "PKG_CONFIG_PATH": "/usr/local/corex/lib64/pkgconfig",
      "PKG_CONFIG": "pkg-config --define-prefix",
      "CGO_CXXFLAGS/CGO_CPPFLAGS": "-I/usr/local/corex/include/opencv4 -D_GLIBCXX_USE_CXX11_ABI=0",
      "CGO_LDFLAGS": "-L/usr/local/corex/lib64"
    },
    "source_changes_required": "none"
  },
  "files": [
    {
      "path": "corex_port/",
      "action": "add",
      "reason": "Migration manifest, per-case test summary, blockers ledger, build command, agent notes (authoritative record)"
    },
    {
      "path": "scripts/build_opencv_corex.sh",
      "action": "add",
      "reason": "SUPERSEDED — from-source OpenCV CUDA build against /usr/local/corex; kept for reference only (OpenCV now pre-installed)"
    },
    {
      "path": "patches/corex/*.hpp",
      "action": "add",
      "reason": "SUPERSEDED — cudev header patches for a from-source OpenCV build; not needed against the pre-installed CoreX OpenCV"
    },
    {
      "path": "Dockerfile.opencv-gpu-corex",
      "action": "add",
      "reason": "Reference image doc for a from-source CoreX OpenCV build (superseded by pre-installed OpenCV)"
    },
    {
      "path": ".gitignore",
      "action": "edit",
      "reason": "Ignore .tools/ (local Go toolchain, gocv-0.31 worktree, old OpenCV build trees)"
    }
  ]
}
